# ocha

思考は英語で行い、ユーザーへのレスポンスは日本語に翻訳すること。
アーティファクトはユーザーにレビューを求める前に日本語に翻訳すること。

タスクを実行中、Stepが10を超えたら一度処理を停止して「完了した作業」「状況の報告」「次のアクション」を報告し、ユーザーの判断を仰ぐこと。
`bun tauri dev` で開発サーバーを、`bun tauri build` で本番用のアプリケーションを構築できる。  
